import React from 'react';

// import "./HeaderSectionItemCard.css"
const HeaderSectionItemCard = ({ item }) => {
    const { text, icon } = item
    const Fab = icon()
    console.log(item)
    return (
        <div>

        </div>
    );
};

export default HeaderSectionItemCard;